- if user_signed_in?
	%p#intro
		Hello
		= current_user.name
		%br/
		%span
			Share Your Inspiration.
-else
	%p#intro
		What's You Muse?
		%br/
		%span
			Write Share.

#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image_url, style: "width: 350px; height: 250px")
			.title
				%h2= link_to post.title, post
			.data.clearfix
				%p.username
					Shared by
					= post.user.name
				%p.buttons
					%span
						%i.fa.fa-comments-o
						= post.comments.count
					%span
						%i.fa.fa-thumbs-o-up
						= post.get_likes.size
					%span
						%i.fa.fa-thumbs-o-down
						= post.get_dislikes.size


/ = link_to "Add New Inspiration", new_post_path
